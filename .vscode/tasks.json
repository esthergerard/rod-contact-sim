{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build",
			"type": "shell",
			"command": "mkdir -p build && cd build && cmake .. && make -j4 && cd ..",
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}